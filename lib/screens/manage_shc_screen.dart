import 'package:flutter/material.dart';

class ManageShcScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('승하차 관리 화면', style: TextStyle(fontSize: 24)),
    );
  }
}
